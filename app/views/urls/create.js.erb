var link = "<%= j render(@url) %>";
<% if current_user %>
  var div_to_append = $("#user_shortened_urls");
<% else %>
  var div_to_append = $("#anonymous_shortened_urls");
<% end %>
div_to_append.append(link);